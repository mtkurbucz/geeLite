% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/startup_diagn.R
\name{StartupDiagn}
\alias{StartupDiagn}
\title{Start-Up Diagnostics}
\usage{
StartupDiagn(target.path = NULL, operation.code = NULL, quiet = FALSE)
}
\arguments{
\item{target.path}{[Optional] Path to the directory. If not provided, the
user is prompted to enter a path. Default value is \code{NULL}.}

\item{operation.code}{[Optional] For updating, use \code{1}, and for
(re)building, use \code{2}. If not provided, the user is prompted to enter a
path. The default value is \code{NULL}.}

\item{quiet}{[Optional] If \code{TRUE}, suppresses prompts or user
interactions. Default value is \code{FALSE}.}
}
\value{
The selected \code{operation.code}. For updating, it is \code{1};
for (re)building, it is \code{2}.
}
\description{
This function performs start-up diagnostics for the GFIData package. It
generates the \code{target.path} and \code{operation.code} global variables
and optionally sets up the configuration file.
}
\examples{
# Example 1: Run start-up diagnostics without providing a target.path
\dontrun{
result <- StartupDiagn()
print(result)
}

# Example 2: Run start-up diagnostics with a target.path
\dontrun{
result <- StartupDiagn(target.path = "/path/to/custom/directory")
print(result)
}

}
